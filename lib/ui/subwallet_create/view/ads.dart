part of "subwallet_create_view.dart";

class _Example extends StatelessWidget {
  const _Example({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text("data");
  }
}
